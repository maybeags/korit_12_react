# 입실 체크 해주세요 !! 🎈

- shoppinglist2 풀이 09:30
- 이미 다 만드신 분들은 그냥 shoppinglist3 프로젝트 만들어서 App.tsx 초기화해놓고 거기다가 쓰겠습니다.

npm create vite@4.4
-> 추가 창에서 npm install
npm install @emotion/react@11.11.1
npm install @emotion/styled@11.11.0
npm install @mui/material@5.14.8

App.tsx를 초기화하고 npm run dev

todolist2 

- 여기에는 기존과 다르게 lucide-react가 아니라 mui 내에서도 아이콘이 존재하기 때문에 추가로 의존성 추가 하겠습니다.
npm install @mui/icons-material@5.16.14

carfront 프로젝트명으로 react/typescript로 만드시고
axios 설치하시고 @tanstack/react-query@4 설치하시오.

App.tsx 초기화하시오.

```tsx
import { Container, AppBar, Toolbar, Typography, CssBaseline } from "@mui/material"

function App() {

  return (
    <Container maxWidth='xl'>
    <CssBaseline />
      <AppBar position="static">
        <Toolbar>
          <Typography variant="h6">
            Car Shop
          </Typography>
        </Toolbar>
      </AppBar>
    </Container>
  )
}

export default App
```
- 저희가 다양한 코드에서 확인했듯이 MUI Container 컴포넌트 내에 AppBar를 하위 컴포넌트로 위치시켰습니다(props drilling 개념 때문에 매우 중요합니다). position prop을 이용하여 AppBar의 위치를 static으로 잡았습니다.
  - static : 스크롤할 때 앱 바가 상단에 고정되지 않고 사라지게 됩니다.
  - fixed : 스크롤 해서 넘어가더라도 앱 바가 상단에 남아있습니다.

  - maxWidth : 영어 읽으면 답 나오고, 이미 shoppinglist/todolist에서 사용했습니다(camelCase인 것만 신경쓰겠습니다).

  - CssBaseline 컴포넌트를 이용하여 여러 브라우저에서 리액트 앱의 형태가 균일하게 유지되도록 설정했습니다. 일반적으로 최상단에 위치시켜 하위 컴포넌트들이 동일한 영향을 받도록 사용합니다.

  - 여기서 제가 AppBar의 position을 속성이라고 하지않고 프롭이라고 이야기한 부분에 좀 주목하셔야 합니다. html에서의 속성은 attribute였습니다. 하지만 props drilling에서의 props의 full word는 properties입니다.


# CRUD 기능 추가하기

## GET 요청하기
- QueryClientProvider 사용하겠습니다.

- 이상의 컴포넌트를 import하겠다고 했다면 다음 작업이 무엇과 관련되었는지 이해할 수 있으면 좋겠습니다.

git add .
git commit -m "feat: 20260312 carfront - GET"
git push
자격 증명
